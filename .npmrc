node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false

# Fija NativeWind a 4.1.23 para evitar que suba a 4.2.x (rompe con RN Reanimated 3.x).
# NativeWind 4.2+ requiere react-native-worklets que no existe en Expo SDK 52.
overrides[nativewind]=4.1.23
